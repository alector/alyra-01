const shortText = 'JS, ECMAScript, esm6 peuvent être considérés comme des alias de Javascript'

myWord = 'Javascript'

if (shortText.includes(myWord)) {
  console.log(`word ${myWord} found!`)
} else {
  console.log(`word ${myWord} NOT found!`)
}
